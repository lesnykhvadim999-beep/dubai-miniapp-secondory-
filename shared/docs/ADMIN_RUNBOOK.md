# Vadim Realty Ecosystem — Admin Runbook

_Auto-generated by `shared/auto_docs_v2/runbook_generator.py` on 2026-06-03 10:19 UTC._

Brand: **Vadim Realty** · RERA BRN **65011**.

## Table of Contents
1. [Architecture Overview](#1-architecture-overview)
2. [Common Issues & Solutions](#2-common-issues--solutions)
3. [Emergency Procedures](#3-emergency-procedures)
4. [Cron Schedule (UTC)](#4-cron-schedule-utc)
5. [Database Schema (public)](#5-database-schema-public)
6. [Free LLM Chain Order](#6-free-llm-chain-order)

## 1. Architecture Overview

### Phase BL
- `shared/fsst_core.py` (module)
- `shared/vadim_pdf.py` (module)
- `shared/subscription.py` (module)

### Phase BM
- `shared/causal_engine` (dir)
- `shared/market_world_model` (dir)
- `shared/agent_bus` (dir)
- `shared/meta_learner` (dir)
- `shared/content_pipeline` (dir)
- `shared/session_continuity` (dir)
- `shared/external_knowledge` (dir)
- `shared/self_modify` (dir)
- `shared/continuous_reasoning` (dir)

### Phase BN
- `shared/immune_system` (dir)
- `shared/autonomous_audit` (dir)
- `shared/optimizer` (dir)
- `shared/safety_nets` (dir)

### Phase BO
- `shared/disaster_recovery` (dir)
- `shared/auto_docs_v2` (dir)

## 2. Common Issues & Solutions

_No bug_kb table found. Hand-curated highlights:_

- **Bot silent / no replies**: check Railway `logs --tail 50`; most often DSN env var missing or LLM chain exhausted (see §6).
- **PDF generation slow**: Cerebras likely degraded — chain falls back to Groq → Together → Anthropic. Verify with `select * from llm_provider_health order by checked_at desc limit 10;`
- **Duplicate listings in staging**: instant-delete should run; if not, check `staging-processor` health.
- **Cron jobs not firing**: inspect `shared/scripts/phase_bm_cron_state.json` and master_cron logs.

## 3. Emergency Procedures

### 3.1 Rollback a deploy (Railway)
```
C:\Temp\railway.exe status
C:\Temp\railway.exe rollback        # interactive picker
# or: redeploy a specific commit
git revert <bad-sha> && git push
```

### 3.2 Restore from backup
- Daily DR backup runs at 01:00 UTC (`dr_backup_daily`).
- Restore script: `python -m shared.disaster_recovery.restore --latest`.
- Verify checksums first with `--db intelligence --bucket daily`.

### 3.3 Disable a feature flag
- Feature flags live in env vars (e.g. `ENABLE_AI_CONSULT=0`).
- `C:\Temp\railway.exe variable set ENABLE_AI_CONSULT=0`
- Bot picks it up on next restart (or auto-restart via `/health`).

### 3.4 Kill a runaway cron job
- `shared/scripts/phase_bm_cron_state.json` holds last_run per job.
- Edit the file or delete the entry to force-skip until the next slot.
- For long-running rogue: kill via Railway dashboard → service → restart.

### 3.5 Database lock storm
- `SELECT pg_terminate_backend(pid) FROM pg_stat_activity WHERE state='active' AND query_start < now() - interval '5 min';`
- Use sparingly — prefer `pg_cancel_backend` first.

## 4. Cron Schedule (UTC)

| Job | Cron | Description |
|-----|------|-------------|
| `agent_j_rss` | `0 */3 * * *` | Agent J Rss |
| `agent_j_telegram` | `0 */2 * * *` | Agent J Telegram |
| `agent_j_meta` | `0 3 * * 0` | Agent J Meta |
| `agent_j_digest` | `0 8 * * *` | Agent J Digest |
| `agent_l_daily_content` | `0 8 * * *` | Agent L Daily Content |
| `agent_l_weekly_top` | `0 9 * * 1` | Agent L Weekly Top |
| `agent_l_monthly_report` | `0 10 1 * *` | Agent L Monthly Report |
| `agent_l_self_modify` | `0 2 * * *` | Agent L Self Modify |
| `agent_l_followups` | `*/5 * * * *` | Agent L Followups |
| `agent_g_weekly_learner` | `0 6 * * 0` | Agent G Weekly Learner |
| `agent_i_mwm_weekly` | `0 2 * * 0` | Agent I Mwm Weekly |
| `agent_k_causal_monthly` | `0 4 1 * *` | Agent K Causal Monthly |
| `agent_h_subscriber` | `* * * * *` | Agent H Subscriber |
| `dr_backup_daily` | `0 1 * * *` | Dr Backup Daily |
| `dr_verify_weekly` | `30 2 * * 0` | Dr Verify Weekly |
| `dr_retention_prune` | `0 4 * * *` | Dr Retention Prune |
| `ux_metrics_daily_funnels` | `0 6 * * *` | Ux Metrics Daily Funnels |
| `ux_rollback_check` | `0 7 * * *` | Ux Rollback Check |
| `immune_diagnose_hourly` | `0 * * * *` | Immune Diagnose Hourly |
| `immune_immunize_daily` | `0 2 * * *` | Immune Immunize Daily |
| `immune_verify_weekly` | `0 3 * * 0` | Immune Verify Weekly |
| `hourly_audit` | `0 * * * *` | Hourly Audit |
| `daily_audit` | `0 3 * * *` | Daily Audit |
| `weekly_audit` | `0 4 * * 0` | Weekly Audit |
| `audit_daily_digest` | `30 8 * * *` | Audit Daily Digest |
| `optimizer_query_perf` | `0 2 * * *` | Optimizer Query Perf |
| `optimizer_dead_code` | `0 5 * * 0` | Optimizer Dead Code |
| `optimizer_dep_scan` | `0 6 * * 0` | Optimizer Dep Scan |
| `auto_docs_help_daily` | `0 7 * * *` | Auto Docs Help Daily |
| `auto_docs_runbook_weekly` | `0 8 * * 0` | Auto Docs Runbook Weekly |
| `auto_docs_quickstart_weekly` | `5 8 * * 0` | Auto Docs Quickstart Weekly |

Add new jobs in `shared/scripts/phase_bm_master_cron.py` (JOBS list) — never schedule outside this registry.
## 5. Database Schema (public)

_Total tables: 25_

| Table | Columns |
|-------|---------|
| `area_period_comparison` | 29 |
| `area_roi_summary` | 18 |
| `backup_runs` | 12 |
| `bot_actions` | 5 |
| `bot_error_events` | 10 |
| `bot_events` | 5 |
| `bot_help_texts` | 4 |
| `bot_leads` | 4 |
| `bot_users` | 8 |
| `building_period_comparison` | 29 |
| `building_roi_summary` | 24 |
| `daily_market_reports` | 10 |
| `docs_inventory` | 4 |
| `intelligence_market_stats` | 8 |
| `intelligence_period_comparison` | 8 |
| `intelligence_rents` | 9 |
| `intelligence_roi` | 8 |
| `intelligence_sales` | 9 |
| `intelligence_status` | 14 |
| `investment_recommendations` | 13 |
| `market_period_summary` | 30 |
| `restore_verifications` | 8 |
| `unit_type_summary` | 11 |
| `user_no_data_events` | 9 |
| `v_system_health` | 9 |

Full per-column definitions: query `information_schema.columns` or use `\d+ <table>` in psql.
## 6. Free LLM Chain Order

All LLM calls go through `shared/llm_chain.py` (synced into each bot). Order:

1. **Cerebras** (`CEREBRAS_API_KEY`) — fastest, free tier, primary.
2. **Groq** (`GROQ_API_KEY`) — fast fallback.
3. **Together AI** (`TOGETHER_API_KEY`).
4. **Gemini** (`GEMINI_API_KEY`) — used for translations / long-context.
5. **OpenRouter** (`OPENROUTER_API_KEY`) — multi-model proxy.
6. **Anthropic** (`ANTHROPIC_API_KEY`) — last-resort paid fallback (cap'd).

Health tracked in `llm_provider_health` table. Provider that returns 5xx /
429 is cooled-down for 5 min then re-tried.

**Never** hard-code a provider — always call `llm_chain.llm_call(prompt)`.


---
_End of runbook. Never paste DSNs / tokens / passwords here — this document is generated and re-generated weekly._